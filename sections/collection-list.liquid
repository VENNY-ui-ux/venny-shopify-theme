{{ 'component-collection-card.css' | asset_url | stylesheet_tag }}
{{ 'component-text-on-image.css' | asset_url | stylesheet_tag }}
{{ 'section-collection-list.css' | asset_url | stylesheet_tag }}
<section id="section-{{ section.id }}" class="Container Container--specialSpacingFirstSection Container--{{ section.settings.section_spacing }} {% if section.settings.section_width == 'full_width' %}Container--fluid{% endif %} CollectionList CollectionList--{{ section.settings.layout_mobile }}" data-section-id="{{ section.id }}">
  {%- assign item_block_count = 0 -%}
  {%- for block in section.blocks -%}
    {%- case block.type -%}
      {%- when 'collection' -%}
        {%- assign item_block_count = item_block_count | plus: 1 -%}
      {%- when 'section_header' -%}
        <header class="SectionHeader SectionHeader--{{ block.settings.header_align }}"  {{ block.shopify_attributes }}>
          <div class="SectionHeader__Text">
            {%- if block.settings.heading != blank -%}
                <h2 class="SectionHeader__Heading CustomSize {{ block.settings.heading_font_size }}">{{ block.settings.heading }}</h2>
            {%- endif -%}

            {%- if block.settings.subheading != blank -%}
                <div class="SectionHeader__Subheading TextMuted">{{ block.settings.subheading }} </div>
            {%- endif -%}
          </div>

          {%- if block.settings.header_align == 'left' and block.settings.button_label != blank -%} 
            <div class="SectionHeader__Button"> 
              <a href="{{ block.settings.button_link }}" class="SectionHeader__Button Button Button--outline">{{ block.settings.button_label | escape }}</a>        
            </div>
          {%- endif -%}

          {%- if block.settings.header_align == 'center' and block.settings.button_label != blank -%} 
            {%- capture section_footer -%}
              <footer class="SectionFooter TextCenter">
                  <a class="SectionFooter__Button Button Button--outline" href="{{ block.settings.button_link }}">{{ block.settings.button_label | escape }}</a>
              </footer>
            {%- endcapture -%}
          {%- endif -%}
        </header>
    {%- endcase -%}
  {%- endfor -%}

  {%- assign desktop_items_per_row = section.settings.grid_desktop_items_per_row -%}
  {%- if desktop_items_per_row > item_block_count -%}
    {%- assign desktop_items_per_row = item_block_count -%}
  {%- endif -%}
  {%- assign mobile_items_per_row  = section.settings.grid_mobile_items_per_row | times: 1 -%}

  <div class="CollectionList__Body">
    <div class="Grid">
      {%- if item_block_count > 0 -%}
        {%- for block in section.blocks -%}
          {%- case block.type -%}
            {%- when 'collection' -%}
              {%- assign collection = collections[block.settings.collection] -%}
              <div class="Grid__Item 1/{{ mobile_items_per_row }}--small 1/{{ desktop_items_per_row }}--medium-up" {{ block.shopify_attributes }}>
                  {%- render 'collection-card', collection: collection, section: section, block: block -%}
              </div>
          {%- endcase -%}
        {%- endfor -%}
      {%- else -%}
        {%- for i in (1..section.settings.grid_desktop_items_per_row) -%}
            <div class="Grid__Item 1/{{ mobile_items_per_row }}--small 1/{{ section.settings.grid_desktop_items_per_row }}--medium-up">
              {%- render 'collection-card-placeholder', section: section -%}
            </div>
        {%- endfor -%}
      {%- endif -%}
    </div>
  </div>

  {{ section_footer }}
</section>

{% schema %}
{
  "name": "Collections list",
  "disabled_on": {
    "groups": [
      "header",
      "footer"
    ]
  },
  "settings": [
    {
      "type": "select",
      "id": "section_width",
      "label": "Section width",
      "options": [
        {
          "value": "default",
          "label": "Grid"
        },
        {
          "value": "full_width",
          "label": "Full width"
        }
      ],
      "default": "default"
    },
    {
      "type": "select",
      "id": "section_spacing",
      "label": "Section spacing",
      "options": [
        {
          "value": "spacingSmall",
          "label": "Small"
        },
        {
          "value": "spacingMedium",
          "label": "Medium"
        },
        {
          "value": "spacingLarge",
          "label": "Large"
        }
      ],
      "default": "spacingMedium"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "label": "Image ratio",
      "options": [
        {
          "value": "natural",
          "label": "Adapt to image"
        },
        {
          "value": "2x3",
          "label": "2:3"
        },
        {
          "value": "3x4",
          "label": "3:4"
        },
        {
          "value": "1x1",
          "label": "1:1"
        },
        {
          "value": "4x3",
          "label": "4:3"
        },
        {
          "value": "16x9",
          "label": "16:9"
        },
        {
          "value": "21x9",
          "label": "21:9"
        }
      ],
      "default": "3x4"
    },
    {
      "type": "select",
      "id": "layout_mobile",
      "label": "Mobile layout",
      "options": [
        {
          "value": "stacked",
          "label": "Stacked"
        },
        {
          "value": "horizontal",
          "label": "Horizontal"
        }
      ],
      "default": "horizontal"
    },
    {
      "type": "header",
      "content": "Grid"
    },
    {
      "type": "range",
      "min": 1,
      "max": 6,
      "step": 1,
      "id": "grid_desktop_items_per_row",
      "label": "Number of columns on desktop",
      "default": 3
    },
    {
      "type": "select",
      "options": [
        {
          "value": "1",
          "label": "1"
        },
        {
          "value": "2",
          "label": "2"
        }
      ],
      "id": "grid_mobile_items_per_row",
      "label": "Number of columns on mobile",
      "info": "Visible when the ‘Mobile layout’ setting is set to Stacked.",
      "default": "2"
    }
  ],
  "blocks": [
    {
      "type": "section_header",
      "name": "Section header",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Heading"
        },
        {
          "type": "select",
          "id": "header_align",
          "label": "Header alignment",
          "options": [
            {
              "value": "center",
              "label": "Center"
            },
            {
              "value": "left",
              "label": "Left"
            }
          ],
          "default": "left"
        },
        {
          "type": "select",
          "id": "heading_font_size",
          "label": "Heading size",
          "options": [
            {
              "value": "HeadlineSmall",
              "label": "Small"
            },
            {
              "value": "HeadlineMedium",
              "label": "Medium"
            },
            {
              "value": "HeadlineLarge",
              "label": "Large"
            }
          ],
          "default": "HeadlineSmall"
        },
        {
          "type": "text",
          "id": "subheading",
          "label": "Subheading"
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button label"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button link"
        }
      ]
    },
    {
      "type": "collection",
      "name": "Collection",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Collection"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image",
          "info": "600 x 800px .jpg recommended."
        },
        {
          "type": "header",
          "content": "Button"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Label"
        },
        {
          "type": "select",
          "id": "heading_style",
          "label": "Style",
          "options": [
            {
              "value": "primary",
              "label": "Primary"
            },
            {
              "value": "secondary",
              "label": "Secondary"
            }
          ],
          "default": "secondary"
        },
        {
          "type": "select",
          "id": "content_position",
          "label": "Position",
          "options": [
            {
              "value": "bottomLeft",
              "label": "Left"
            },
            {
              "value": "bottomCenter",
              "label": "Center"
            },
            {
              "value": "bottomRight",
              "label": "Right"
            }
          ],
          "default": "bottomCenter"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Collection list",
      "settings": {
      },
      "blocks": [
        {
          "type": "collection"
        },
        {
          "type": "collection"
        },
        {
          "type": "collection"
        }
      ]
    }
  ]
}
{% endschema %}
